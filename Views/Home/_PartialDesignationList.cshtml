@model List<AdminPannel.Models.DesignationResponse>

@if(Model == null || !Model.Any())
{
<div class="alert alert-info">No designations found.</div>
}
else
{
<div class="table-responsive">
    <table class="table table-sm table-striped">
        <thead class="table-dark">
            <tr>
                <th style="width: 5%">#</th>
                <th style="width: 25%">Designation Name</th>
                <th style="width: 20%">Designation Code</th>
                <th style="width: 35%">Description</th>
                <th style="width: 15%">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr class="align-middle">
                    <td>@(Model.IndexOf(item) + 1)</td>
                    <td><strong>@item.DesignationName</strong></td>
                    <td>@item.DesignationCode</td>
                    <td>@item.Description</td>
                    <td>
                        @if (User.IsInRole("Admin") || User.IsInRole("HR"))
                        {
                        <div class="btn-group btn-group-sm" role="group">
                            <button type="button" class="btn btn-outline-success btn-sm" onclick="EditDesignation(@item.DesignationID)" title="Edit">
                                <i class="bi bi-pencil"></i> Edit
                            </button>
                            <button type="button" class="btn btn-outline-danger btn-sm" onclick="DeleteDesignation(@item.DesignationID)" title="Delete">
                                <i class="bi bi-trash"></i> Delete
                            </button>
                        </div>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
}
